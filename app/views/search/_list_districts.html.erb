<%= label_tag 'district_name', 'District(s)' %>
<%= select_tag 'districts',  options_from_collection_for_select(districts, :id, :name, :selected => selected_districts), {:size => 8, :multiple => true} %>

